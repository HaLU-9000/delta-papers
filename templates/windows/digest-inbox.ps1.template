# Auto-generated by scheduler.py. Polls Gmail INBOX for /digest commands.
$ErrorActionPreference = 'Continue'
$LogDir = Join-Path $env:USERPROFILE ".claude\skills\digest\reports"
if (-not (Test-Path $LogDir)) { New-Item -ItemType Directory -Force -Path $LogDir | Out-Null }
$LogFile = Join-Path $LogDir "inbox.log"
$ScriptPath = Join-Path $env:USERPROFILE ".claude\skills\digest\scripts\inbox_poll.py"
& python "$ScriptPath" --once *>> $LogFile
