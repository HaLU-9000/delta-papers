# Auto-generated by scheduler.py. Runs the ΔPapers morning digest.
$ErrorActionPreference = 'Continue'
$LogDir = Join-Path $env:USERPROFILE ".claude\skills\digest\reports"
if (-not (Test-Path $LogDir)) { New-Item -ItemType Directory -Force -Path $LogDir | Out-Null }
$LogFile = Join-Path $LogDir "cron.log"
& '{CLAUDE_BIN}' -p "/digest run" *>> $LogFile
